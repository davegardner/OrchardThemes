@*I can find no other way to reduce the textarea row attribute from 25 to 5 than to override this template.
    It is probably possible to do something with placement and should look at trying to do that as this fix for the Contact page
    will screw up any other front-end page that uses a body part
    
    DAG 24/7/2015
    *@


@using Orchard.Utility.Extensions;
@{
    string editorFlavor = Model.EditorFlavor;

    var htmlAttributes = new Dictionary<string, object> {
        {"class", editorFlavor.HtmlClassify()}
    };

    if (Model.Required == true) {
        htmlAttributes["required"] = "required";
    }

    if (Model.AutoFocus == true) {
        htmlAttributes["autofocus"] = "autofocus";
    }
}

@Html.TextArea("Text", (string)Model.Text, 5, 80, htmlAttributes)